services:
  lambda:
    build:
      context: .
      dockerfile: Dockerfile
    image: node-aws-lambda:latest
    container_name: node-aws-lambda-local
    ports:
      - "9000:8080"
    environment:
      - NODE_ENV=development
